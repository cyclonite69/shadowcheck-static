# Cruft Audit — 2026-06-20

Generated: 2026-06-20

Git HEAD: `8a19e921`

Mode: static, read-only analysis; findings are candidates, not deletion or refactor authorization.

## Scope and Method

The generator reads `package.json`, TypeScript configs, env example key names, and static imports/exports across configured source roots. It does not delete files, change dependencies, inspect secret values, or claim dynamic runtime reachability.

## Summary

| Category           | Candidates |
| ------------------ | ---------: |
| Unreferenced files |         34 |
| Unused exports     |         88 |
| Orphan tests       |          9 |
| Dependencies       |         11 |
| Environment keys   |         86 |
| Temporary files    |          0 |

## Unreferenced File Candidates

| Severity | Confidence | Path                                                                 | Evidence                                                                              | Recommended verification                                                                             |
| -------- | ---------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| MEDIUM   | medium     | `client/src/components/ActiveFiltersSummary.tsx`                     | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/admin/hooks/useWigleDetailData.ts`            | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/admin/tabs/data-import/ObservationsPanel.tsx` | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/admin/tabs/wigle-detail/SearchControls.tsx`   | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/contextMenu/NetworkContextMenuMenu.tsx`       | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/contextMenu/NetworkContextMenuTable.tsx`      | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/contextMenu/NetworkContextNoteModal.tsx`      | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/contextMenu/NetworkContextNotes.tsx`          | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/geospatial/hooks/contextMenu/validators.ts`   | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/geospatial/markers/markerComponents.ts`       | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/kepler/index.ts`                              | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/wigle/hooks/useWigleMapState.ts`              | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/components/wigle/index.ts`                               | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/hooks/useFilteredData.ts`                                | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/hooks/useNetworkNotes.ts`                                | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/utils/geospatial/setupPopupTether.ts`                    | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/utils/macUtils.ts`                                       | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `client/src/utils/mapboxLoader.ts`                                   | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/fbi-field-offices-gov.ts`                                  | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/fbi-offices.ts`                                            | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/fbi-resident-agencies-foia.ts`                             | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/fbi-resident-agencies-gov-legacy.ts`                       | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/fbi-resident-agencies-gov.ts`                              | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/fbi-resident-agencies-public.ts`                           | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/fbi-training-facilities.ts`                                | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/json-import.ts`                                            | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/kismet-import.ts`                                          | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/kml-import.ts`                                             | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/load/sqlite-import-incremental.ts`                              | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/promote/refresh-mviews.ts`                                      | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/promote/run-scoring.ts`                                         | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/promote/validate-data.ts`                                       | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/repair/repair-missing-networks.ts`                              | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |
| MEDIUM   | medium     | `etl/run-pipeline.ts`                                                | No static inbound import or require reference was found in audited source/test roots. | Verify runtime registration, dynamic loading, CLI use, and documentation references before deleting. |

## Unused Export Candidates

| Severity | Confidence | Path                                                                    | Evidence                                                                                             | Recommended verification                                                                                     |
| -------- | ---------- | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| LOW      | medium     | `client/src/components/DashboardPage.tsx`                               | Export `DashboardPage` has no matching named static import among 1 importer(s).                      | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/LazyMapComponent.tsx`                            | Export `LazyMapComponent` has no matching named static import among 1 importer(s).                   | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/StartPage.tsx`                                   | Export `StartPage` has no matching named static import among 1 importer(s).                          | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/admin/hooks/apiTestingPresets.ts`                | Export `API_PRESETS` has no matching named static import among 4 importer(s).                        | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/admin/hooks/useApiTesting.ts`                    | Export `ApiInput` has no matching named static import among 1 importer(s).                           | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/admin/hooks/useApiTesting.ts`                    | Export `ApiPreset` has no matching named static import among 1 importer(s).                          | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/admin/hooks/useApiTesting.ts`                    | Export `categorizeStatus` has no matching named static import among 1 importer(s).                   | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/analytics/AnalyticsPage.tsx`                     | Export `Analytics` has no matching named static import among 1 importer(s).                          | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/analytics/utils/chartConfig.tsx`                 | Export `PIE_CONFIG` has no matching named static import among 1 importer(s).                         | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/analytics/utils/chartConfig.tsx`                 | Export `getResponsiveContainerKey` has no matching named static import among 1 importer(s).          | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/analytics/utils/chartConstants.tsx`              | Export `FilterIcon` has no matching named static import among 5 importer(s).                         | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/analytics/utils/chartHelpers.ts`                 | Export `calculatePercentage` has no matching named static import among 1 importer(s).                | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/analytics/utils/chartHelpers.ts`                 | Export `formatNumber` has no matching named static import among 1 importer(s).                       | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/analytics/utils/chartHelpers.ts`                 | Export `getChartKey` has no matching named static import among 1 importer(s).                        | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `PaletteEntry` has no matching named static import among 2 importer(s).                       | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `PaletteGroup` has no matching named static import among 2 importer(s).                       | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `ResolvedBadgeColors` has no matching named static import among 2 importer(s).                | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `SEMANTIC_PALETTE` has no matching named static import among 2 importer(s).                   | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `autoContrastText` has no matching named static import among 2 importer(s).                   | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `hexToHsl` has no matching named static import among 2 importer(s).                           | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `hexToRgb` has no matching named static import among 2 importer(s).                           | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `hexWithOpacity` has no matching named static import among 2 importer(s).                     | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `hslToHex` has no matching named static import among 2 importer(s).                           | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `matchesRule` has no matching named static import among 2 importer(s).                        | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `paletteByToken` has no matching named static import among 2 importer(s).                     | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/badgeStudio/index.ts`                            | Export `resolveBadgeColors` has no matching named static import among 2 importer(s).                 | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/dashboard/MetricCard.tsx`                        | Export `MetricCard` has no matching named static import among 2 importer(s).                         | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/filter/index.ts`                                 | Export `FilterInputProps` has no matching named static import among 11 importer(s).                  | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/filter/index.ts`                                 | Export `FilterSectionProps` has no matching named static import among 11 importer(s).                | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/geospatial/contextMenuUtils.ts`                  | Export `fetchNetworkTagAndNotes` has no matching named static import among 1 importer(s).            | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/geospatial/hooks/useNearestCourthouses.ts`       | Export `CourthouseMatch` has no matching named static import among 1 importer(s).                    | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/geospatial/hooks/useRadiusFilterLayer.ts`        | Export `formatRadius` has no matching named static import among 1 importer(s).                       | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/geospatial/hooks/useRadiusFilterPopup.ts`        | Export `buildPopupHTML` has no matching named static import among 1 importer(s).                     | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/geospatial/toolbar/MapToolbar.tsx`               | Export `Separator` has no matching named static import among 1 importer(s).                          | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/geospatial/utils/siblingGroupGraph.ts`           | Export `parseQuickSearch` has no matching named static import among 4 importer(s).                   | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/geospatial/utils/siblingGroupGraph.ts`           | Export `regroupSiblingNetworks` has no matching named static import among 4 importer(s).             | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/geospatial/utils/siblingGroupGraph.ts`           | Export `serializeGroupMap` has no matching named static import among 4 importer(s).                  | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/hooks/useAgencyOffices.ts`                       | Export `ensureAgencyOfficeLayers` has no matching named static import among 4 importer(s).           | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/hooks/useFederalCourthouses.ts`                  | Export `ensureFederalCourthouseLayers` has no matching named static import among 3 importer(s).      | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/vendor-intel/VendorIntelDrawer.tsx`              | Export `VENDOR_INTEL_EVENT` has no matching named static import among 3 importer(s).                 | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/wigle/kmlLayers.ts`                              | Export `updateKmlLayerData` has no matching named static import among 2 importer(s).                 | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/components/wigle/mapLayers.ts`                              | Export `FIELD_DATA_LAYER` has no matching named static import among 4 importer(s).                   | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/constants/network.ts`                                       | Export `NETWORK_COLORS` has no matching named static import among 34 importer(s).                    | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/hooks/authController.ts`                                    | Export `default` has no matching named static import among 6 importer(s).                            | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/hooks/authController.ts`                                    | Export `isAuthenticationRequest` has no matching named static import among 6 importer(s).            | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/hooks/useAdminRuntimeConfig.ts`                             | Export `ADMIN_RUNTIME_CONFIG_CHANGED_EVENT` has no matching named static import among 2 importer(s). | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/hooks/useNetworkData.ts`                                    | Export `default` has no matching named static import among 1 importer(s).                            | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/hooks/useObservations.ts`                                   | Export `default` has no matching named static import among 1 importer(s).                            | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/utils/filterCapabilities.ts`                                | Export `PAGE_CAPABILITIES` has no matching named static import among 6 importer(s).                  | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/utils/filterCapabilities.ts`                                | Export `createBasicCapabilities` has no matching named static import among 6 importer(s).            | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/utils/filterCapabilities.ts`                                | Export `createFullCapabilities` has no matching named static import among 6 importer(s).             | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/utils/geospatial/fieldFormatting.ts`                        | Export `formatCoordinates` has no matching named static import among 3 importer(s).                  | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/utils/geospatial/fieldFormatting.ts`                        | Export `formatHeading` has no matching named static import among 3 importer(s).                      | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/utils/geospatial/fieldFormatting.ts`                        | Export `formatTimestamp` has no matching named static import among 3 importer(s).                    | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/utils/geospatial/setupPopupPin.ts`                          | Export `cleanupAllPinStates` has no matching named static import among 5 importer(s).                | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/utils/geospatial/setupPopupPin.ts`                          | Export `isPopupPinned` has no matching named static import among 5 importer(s).                      | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `client/src/utils/icons/radioTypeIcons.ts`                              | Export `RADIO_TYPE_ICONS` has no matching named static import among 1 importer(s).                   | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `etl/transform/enrich-agency-offices-zip4-smarty.ts`                    | Export `enrichZip4` has no matching named static import among 1 importer(s).                         | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/api/routes/v1/admin/adminGeocodingHelpers.ts`               | Export `API_KEY_PROVIDERS` has no matching named static import among 2 importer(s).                  | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/api/routes/v2/filtered/types.ts`                            | Export `EnabledFlags` has no matching named static import among 7 importer(s).                       | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/api/routes/v2/filtered/types.ts`                            | Export `Filters` has no matching named static import among 7 importer(s).                            | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/core/initialization/appInit.ts`                             | Export `AppInitResult` has no matching named static import among 1 importer(s).                      | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/core/initialization/appInit.ts`                             | Export `ServerConfig` has no matching named static import among 1 importer(s).                       | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/core/initialization/credentialsInit.ts`                     | Export `SecretsManager` has no matching named static import among 3 importer(s).                     | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/core/initialization/middlewareInit.ts`                      | Export `MiddlewareOptions` has no matching named static import among 1 importer(s).                  | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/core/initialization/routesInit.ts`                          | Export `InitializeRoutesOptions` has no matching named static import among 1 importer(s).            | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/core/initialization/routesInit.ts`                          | Export `RouteModules` has no matching named static import among 1 importer(s).                       | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/admin/networkTagCore.ts`                           | Export `exportMLTrainingData` has no matching named static import among 1 importer(s).               | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/admin/networkTagCore.ts`                           | Export `getNetworksPendingWigleLookup` has no matching named static import among 1 importer(s).      | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/admin/siblingDetectionConstants.ts`                | Export `FLEET_SSIDS` has no matching named static import among 3 importer(s).                        | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/backup/postgresToolchain.ts`                       | Export `isLocalComposePostgres` has no matching named static import among 1 importer(s).             | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/backup/postgresToolchain.ts`                       | Export `runDockerizedPgDump` has no matching named static import among 1 importer(s).                | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/backup/postgresToolchain.ts`                       | Export `runNativePgDump` has no matching named static import among 1 importer(s).                    | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/explorerSorting.ts`                                | Export `NETWORKS_SORT_MAP` has no matching named static import among 2 importer(s).                  | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/explorerSorting.ts`                                | Export `NETWORKS_V2_SORT_MAP` has no matching named static import among 2 importer(s).               | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/filterQueryBuilder/NetworkWhereBuildContext.ts`    | Export `AppliedFilter` has no matching named static import among 2 importer(s).                      | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/visint/visintPipeline.ts`                          | Export `generateThumbnail` has no matching named static import among 2 importer(s).                  | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/wigleEnrichment/use-cases/startBatchEnrichment.ts` | Export `MAX_TARGETED_ENRICHMENT_BATCH` has no matching named static import among 1 importer(s).      | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/services/wigleImport/mappers/buildCreateImportRunInput.ts`  | Export `CreateImportRunInput` has no matching named static import among 3 importer(s).               | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/utils/databaseSetup.ts`                                     | Export `DatabaseConnectionResult` has no matching named static import among 1 importer(s).           | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/utils/queryPerformanceTracker.ts`                           | Export `FilterMetrics` has no matching named static import among 3 importer(s).                      | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/utils/queryPerformanceTracker.ts`                           | Export `QueryMetrics` has no matching named static import among 3 importer(s).                       | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/utils/serverDependencies.ts`                                | Export `CoreDependencies` has no matching named static import among 1 importer(s).                   | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/utils/serverDependencies.ts`                                | Export `Logger` has no matching named static import among 1 importer(s).                             | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/utils/serverDependencies.ts`                                | Export `RouteModules` has no matching named static import among 1 importer(s).                       | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/utils/serverLifecycle.ts`                                   | Export `LifecycleOptions` has no matching named static import among 1 importer(s).                   | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/utils/serverLifecycle.ts`                                   | Export `Logger` has no matching named static import among 1 importer(s).                             | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |
| LOW      | medium     | `server/src/utils/serverStartup.ts`                                     | Export `StartupOptions` has no matching named static import among 1 importer(s).                     | Confirm there is no dynamic, reflective, test-only, or external package consumer before removing the export. |

## Orphan Test Candidates

| Severity | Confidence | Path                                                | Evidence                                                                         | Recommended verification                                                                                                    |
| -------- | ---------- | --------------------------------------------------- | -------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| LOW      | low        | `tests/integration/etl/pipeline-integrity.test.ts`  | No statically resolved import or require target outside the test tree was found. | Inspect mocks, global setup, route registration, and string-based dynamic imports before classifying this test as orphaned. |
| LOW      | low        | `tests/integration/networks-data-integrity.test.ts` | No statically resolved import or require target outside the test tree was found. | Inspect mocks, global setup, route registration, and string-based dynamic imports before classifying this test as orphaned. |
| LOW      | low        | `tests/unit/bleSecurityClassification.test.ts`      | No statically resolved import or require target outside the test tree was found. | Inspect mocks, global setup, route registration, and string-based dynamic imports before classifying this test as orphaned. |
| LOW      | low        | `tests/unit/calculateThreatScoreV5Sql.test.ts`      | No statically resolved import or require target outside the test tree was found. | Inspect mocks, global setup, route registration, and string-based dynamic imports before classifying this test as orphaned. |
| LOW      | low        | `tests/unit/geospatial/radiusFilter.test.ts`        | No statically resolved import or require target outside the test tree was found. | Inspect mocks, global setup, route registration, and string-based dynamic imports before classifying this test as orphaned. |
| LOW      | low        | `tests/unit/isLinearTravelPair.test.ts`             | No statically resolved import or require target outside the test tree was found. | Inspect mocks, global setup, route registration, and string-based dynamic imports before classifying this test as orphaned. |
| LOW      | low        | `tests/unit/property/sqlBuilder.fuzz.test.ts`       | No statically resolved import or require target outside the test tree was found. | Inspect mocks, global setup, route registration, and string-based dynamic imports before classifying this test as orphaned. |
| LOW      | low        | `tests/unit/threatCategoryFilter.test.ts`           | No statically resolved import or require target outside the test tree was found. | Inspect mocks, global setup, route registration, and string-based dynamic imports before classifying this test as orphaned. |
| LOW      | low        | `tests/wigle-import-auth.test.ts`                   | No statically resolved import or require target outside the test tree was found. | Inspect mocks, global setup, route registration, and string-based dynamic imports before classifying this test as orphaned. |

## Dependency Candidates

| Severity | Confidence | Path           | Evidence                                                                                      | Recommended verification                                                                           |
| -------- | ---------- | -------------- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| LOW      | low        | `package.json` | Dependency `@deck.gl/core` has no static import/require reference in audited code roots.      | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |
| LOW      | low        | `package.json` | Dependency `@deck.gl/layers` has no static import/require reference in audited code roots.    | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |
| LOW      | low        | `package.json` | Dependency `@deck.gl/react` has no static import/require reference in audited code roots.     | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |
| LOW      | low        | `package.json` | Dependency `@deck.gl/widgets` has no static import/require reference in audited code roots.   | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |
| LOW      | low        | `package.json` | Dependency `express-fileupload` has no static import/require reference in audited code roots. | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |
| LOW      | low        | `package.json` | Dependency `keytar` has no static import/require reference in audited code roots.             | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |
| LOW      | low        | `package.json` | Dependency `node-cron` has no static import/require reference in audited code roots.          | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |
| LOW      | low        | `package.json` | Dependency `node-fetch` has no static import/require reference in audited code roots.         | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |
| LOW      | low        | `package.json` | Dependency `preact` has no static import/require reference in audited code roots.             | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |
| LOW      | low        | `package.json` | Dependency `react-is` has no static import/require reference in audited code roots.           | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |
| LOW      | low        | `package.json` | Dependency `react-window` has no static import/require reference in audited code roots.       | Check runtime plugins, native loading, build configuration, and deployment scripts before removal. |

## Environment and Config Drift

Only environment key names are compared. Values are never read or emitted.

| Severity | Confidence | Path           | Evidence                                                                                                                            | Recommended verification                                                                               |
| -------- | ---------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| MEDIUM   | high       | `.env.example` | Environment key `ADMIN_ALLOW_DOCKER` is referenced in code but absent from checked-in env example files.                            | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `ADMIN_ALLOW_ML_SCORING` is referenced in code but absent from checked-in env example files.                        | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `ADMIN_ALLOW_ML_TRAINING` is referenced in code but absent from checked-in env example files.                       | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `ALLOW_INDEXING` is referenced in code but absent from checked-in env example files.                                | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `ALLOW_MOBILE_INGEST_AUTO_PROCESS` is referenced in code but absent from checked-in env example files.              | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `API_URL` is referenced in code but absent from checked-in env example files.                                       | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `AWS_PROFILE` is referenced in code but absent from checked-in env example files.                                   | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `AWS_WEB_IDENTITY_TOKEN_FILE` is referenced in code but absent from checked-in env example files.                   | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `BACKUP_CRON` is referenced in code but absent from checked-in env example files.                                   | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `BACKUP_DIR` is referenced in code but absent from checked-in env example files.                                    | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `BACKUP_RETENTION_DAYS` is referenced in code but absent from checked-in env example files.                         | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `BACKUP_SOURCE_ENV` is referenced in code but absent from checked-in env example files.                             | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `BITWARDENCLI_APPDATA_DIR` is referenced in code but absent from checked-in env example files.                      | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `COOKIE_SECURE` is referenced in code but absent from checked-in env example files.                                 | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DATABASE_URL` is referenced in code but absent from checked-in env example files.                                  | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DB_ADMIN_PASSWORD` is referenced in code but absent from checked-in env example files.                             | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DB_APP_NAME` is referenced in code but absent from checked-in env example files.                                   | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DB_PASSWORD` is referenced in code but absent from checked-in env example files.                                   | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DB_SEARCH_PATH` is referenced in code but absent from checked-in env example files.                                | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DB_SSL` is referenced in code but absent from checked-in env example files.                                        | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DB_SSL_CA` is referenced in code but absent from checked-in env example files.                                     | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DB_SSL_REJECT_UNAUTHORIZED` is referenced in code but absent from checked-in env example files.                    | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DEBUG` is referenced in code but absent from checked-in env example files.                                         | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DEBUG_GEOSPATIAL` is referenced in code but absent from checked-in env example files.                              | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `DEBUG_QUERY_PERFORMANCE` is referenced in code but absent from checked-in env example files.                       | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `EC2_INSTANCE_ID` is referenced in code but absent from checked-in env example files.                               | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `ENABLE_BACKGROUND_JOBS` is referenced in code but absent from checked-in env example files.                        | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `ENV_KEY` is referenced in code but absent from checked-in env example files.                                       | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `FBI_MAX_RETRIES` is referenced in code but absent from checked-in env example files.                               | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `FBI_REQUEST_DELAY_MS` is referenced in code but absent from checked-in env example files.                          | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `FBI_USE_JINA` is referenced in code but absent from checked-in env example files.                                  | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `FORCE_AWS_SM` is referenced in code but absent from checked-in env example files.                                  | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `FULL_EXPORT_MAX_ROWS_PER_TABLE` is referenced in code but absent from checked-in env example files.                | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `FULL_EXPORT_MAX_ROWS_TOTAL` is referenced in code but absent from checked-in env example files.                    | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `HERE_API_KEY` is referenced in code but absent from checked-in env example files.                                  | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `HOST` is referenced in code but absent from checked-in env example files.                                          | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `IMPORT_BATCH_SIZE` is referenced in code but absent from checked-in env example files.                             | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `JEST_WORKER_ID` is referenced in code but absent from checked-in env example files.                                | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `KML_IMPORT_PREFIX` is referenced in code but absent from checked-in env example files.                             | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `LOCATIONIQ_API_KEY` is referenced in code but absent from checked-in env example files.                            | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `LOG_LEVEL` is referenced in code but absent from checked-in env example files.                                     | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `MAPBOX_PER_MINUTE` is referenced in code but absent from checked-in env example files.                             | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `MAPBOX_UNLIMITED_API_KEY` is referenced in code but absent from checked-in env example files.                      | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `ML_AUTO_SCORE_LIMIT` is referenced in code but absent from checked-in env example files.                           | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `ML_MODEL_VERSION` is referenced in code but absent from checked-in env example files.                              | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `ML_SCORE_LIMIT` is referenced in code but absent from checked-in env example files.                                | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `MOBILE_INGEST_STUCK_THRESHOLD_MINUTES` is referenced in code but absent from checked-in env example files.         | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `MOBILE_INGEST_STUCK_THRESHOLD_MINUTES_DEFAULT` is referenced in code but absent from checked-in env example files. | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `MV_REFRESH_CRON` is referenced in code but absent from checked-in env example files.                               | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `OPENCAGE_API_KEY` is referenced in code but absent from checked-in env example files.                              | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGADMIN_COMPOSE_FILE` is referenced in code but absent from checked-in env example files.                          | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGADMIN_CONTAINER_NAME` is referenced in code but absent from checked-in env example files.                        | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGADMIN_DOCKER_HOST_LABEL` is referenced in code but absent from checked-in env example files.                     | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGADMIN_EMAIL` is referenced in code but absent from checked-in env example files.                                 | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGADMIN_PASSWORD` is referenced in code but absent from checked-in env example files.                              | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGADMIN_PORT` is referenced in code but absent from checked-in env example files.                                  | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGADMIN_SERVICE_NAME` is referenced in code but absent from checked-in env example files.                          | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGADMIN_STATUS_PROBE_URL` is referenced in code but absent from checked-in env example files.                      | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGADMIN_URL` is referenced in code but absent from checked-in env example files.                                   | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGADMIN_VOLUME_NAME` is referenced in code but absent from checked-in env example files.                           | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGDATABASE` is referenced in code but absent from checked-in env example files.                                    | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGHOST` is referenced in code but absent from checked-in env example files.                                        | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGPORT` is referenced in code but absent from checked-in env example files.                                        | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PGUSER` is referenced in code but absent from checked-in env example files.                                        | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `PG_DUMP_PATH` is referenced in code but absent from checked-in env example files.                                  | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `POSTGRES_CONTAINER` is referenced in code but absent from checked-in env example files.                            | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `RUN_INTEGRATION_TESTS` is referenced in code but absent from checked-in env example files.                         | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `S3_BACKUP_BUCKET` is referenced in code but absent from checked-in env example files.                              | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SHADOWCHECK_AWS_SECRET` is referenced in code but absent from checked-in env example files.                        | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SIBLING_REFRESH_PAIR_AUDIT` is referenced in code but absent from checked-in env example files.                    | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SILENT_TESTS` is referenced in code but absent from checked-in env example files.                                  | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SIMPLE_RULE_SCORING_ENABLED` is referenced in code but absent from checked-in env example files.                   | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SITE_URL` is referenced in code but absent from checked-in env example files.                                      | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SLOW_FILTERED_QUERY_MS` is referenced in code but absent from checked-in env example files.                        | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SLOW_FILTERED_TOTAL_MS` is referenced in code but absent from checked-in env example files.                        | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SLOW_GEOSPATIAL_QUERY_MS` is referenced in code but absent from checked-in env example files.                      | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SLOW_QUERY_THRESHOLD_MS` is referenced in code but absent from checked-in env example files.                       | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SMARTY_AUTH_ID` is referenced in code but absent from checked-in env example files.                                | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `SMARTY_AUTH_TOKEN` is referenced in code but absent from checked-in env example files.                             | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `TRACK_QUERY_PERFORMANCE` is referenced in code but absent from checked-in env example files.                       | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `WIGLE_DETAIL_IMPORT_DEDUPE_HOURS` is referenced in code but absent from checked-in env example files.              | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `WIGLE_SEARCH_CACHE_TTL_MS` is referenced in code but absent from checked-in env example files.                     | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `WIGLE_SEARCH_NEGATIVE_CACHE_TTL_MS` is referenced in code but absent from checked-in env example files.            | Document the key name and safe placeholder only; never copy a real secret value.                       |
| MEDIUM   | high       | `.env.example` | Environment key `WIGLE_UNSAFE_BULK_DAEMON` is referenced in code but absent from checked-in env example files.                      | Document the key name and safe placeholder only; never copy a real secret value.                       |
| LOW      | low        | `.env.example` | Documented environment key `DOTENV_CONFIG_QUIET` has no direct process.env reference in audited code roots.                         | Check compose interpolation, shell scripts, deployment tooling, and external consumers before removal. |
| LOW      | low        | `.env.example` | Documented environment key `GOOGLE_MAPS_API_KEY` has no direct process.env reference in audited code roots.                         | Check compose interpolation, shell scripts, deployment tooling, and external consumers before removal. |

## Temporary and Campaign File Candidates

_No findings in this category._

## Required Human Gate

No candidate in this report is approved for deletion. Confirm dynamic imports, CLI entrypoints, route mounting, deployment references, and focused tests before any removal.
